# Podcast Media Audit

The audit pipeline is evidence-first. A successful HTTP response is insufficient by itself; the stored series, episode, platform, destination type, title, number, date, guest/subject, and media identifier are compared with captured destination evidence. Results are immutable evidence inputs to proposals, not direct edits.
