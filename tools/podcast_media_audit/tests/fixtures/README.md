# Compatibility fixture

`v23_5c_1_validation_jobs.json` is the production job inventory captured from the first deployed V23.5C.2 connectivity run. It is retained only as a schema and compatibility fixture; the GitHub Actions workflow should use the current inventory generated by the deployed repository.
